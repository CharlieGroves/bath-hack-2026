# Copy to .env and fill in. Never commit .env (it is gitignored).

# Google Cloud / Maps Platform — Places API (Find Place + Place Details)
GOOGLE_API_KEY=
TRAVELTIME_API_KEY=your_traveltime_api_key
TRAVELTIME_APP_ID=your_traveltime_app_id

# Optional: Python for scripts/embed_mpnet.py and embed_openclip_images.py
# PYTHON_BIN=/usr/bin/python3
